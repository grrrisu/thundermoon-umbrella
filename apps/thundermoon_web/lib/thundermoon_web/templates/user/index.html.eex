<section class="row">
  <article class="column">
    <h1>Users</h1>

    <table>
      <thead>
        <tr>
          <th></th>
          <th>Username</th>
          <th></th>
        </tr>
      </thead>
      <tbody>
        <%= for user <- @users do %>
        <tr>
          <td>
            <%= img_tag user.avatar, class: "avatar" %>
          </td>
          <td>
            <%= user.username %>
          </td>
          <td>
            <%= if can? @current_user, :edit, user do %>
              <%= link "edit", to: Routes.user_path(@conn, :edit, user) %>
            <% end %>
            <%= if can? @current_user, :delete, user do %>
              &nbsp;|&nbsp;
              <%= link "delete", to: Routes.user_path(@conn, :delete, user), method: :delete, data: [confirm: "Do you really want to delete user #{user.username}"] %>
            <% end %>
          </td>
        </tr>
        <% end %>
      </tbody>
    </table>
  </article>
</section>

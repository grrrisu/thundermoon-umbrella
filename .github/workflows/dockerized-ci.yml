name: Dockerized CI

on: push

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v1
    - name: prepare containers
      run: docker build -t thundermoon:builder --target=builder .

build:
  docker:
    web: 
      dockerfile: Dockerfile
      target: runner
setup:
  addons:
  - plan: heroku-postgresql
    as: DATABASE
# release:
#   image: web
#   command:
#     - mix ecto.migrate
# run:
#   web: mix phx.server
